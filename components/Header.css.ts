import { style } from '@vanilla-extract/css';

const header = style({
  display: 'flex',
  justifyContent: 'space-between',
  alignItems: 'center',
  padding: '3rem 3rem 0',
  width: '100%',
  height: 'auto',
});

export { header };
