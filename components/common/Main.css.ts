import { style } from '@vanilla-extract/css';

const main = style({
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'center',
  alignItems: 'flex-start',
  width: 'auto',
  height: '100%',
  padding: '0 3rem',
});

export { main };
